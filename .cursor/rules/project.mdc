---
description: pearls-migrator stack, ESM/Zod/Prisma conventions, parser tests, AI metadata
alwaysApply: true
---

# pearls-migrator

Stack: TypeScript 6.x, Node.js 24 (ESM), Prisma (Postgres), Zod 4.x, node:test via `tsx --test`.
Goal: import `.docx`, parse and normalize pearls, generate download archives, serve the web catalog.

## Code style

- ESM only (`import` / `export`). Never use `require()`.
- Strict TypeScript. Keep CLI scripts in `src/cli/` modular; avoid duplicated boilerplate.

## Parsing and persistence

- Always validate Word-derived data with Zod schemas before writing to Prisma.
- New parser features must have tests using `import { test, describe } from 'node:test'`.
- Catalog newest-first order lives only in `src/catalogOrder.ts`. Do not add a second `orderBy` in `web/lib/pearls.ts` or a parallel CLI catalog mapper.

## Web catalog / SEO

- Catalog and pearl pages stay server-rendered (`force-dynamic`). Crawlers must receive full HTML. Do not convert the homepage to client-only rendering or ISR/cache unless TTFB is measured and SEO is preserved.
- The year dropdown is session UI only: it must not persist in the URL, so a reload shows all years again.

## AI metadata enrichment

- In `enrichMetadataWithAi.ts` (and related helpers): use typed OpenAI structured output bound to a Zod schema.
