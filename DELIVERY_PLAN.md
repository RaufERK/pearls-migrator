# Pearls Migrator — Delivery Plan

Этот файл фиксирует порядок ближайших спринтов, релизов и коммитов.

`ARCHITECTURE.md` описывает целевую архитектуру. `DELIVERY_PLAN.md` описывает, как идти к ней маленькими проверяемыми шагами.

## Рабочий Принцип

- Русские Word-брошюры в `data/source-data/pearls-word/` — основной источник контента для нового парсинга.
- PDF-файлы хранятся в `data/source-data/pearls-pdf/` как архив старых оригиналов, но больше не являются источником парсинга.
- Подготовленные `.docx` складываются в `data/word-docx/{year}/...` как промежуточный результат конвертации.
- На первом этапе парсим только папки `Брошюры` / `БРОШЮРЫ`, не папки `Оригиналы`.
- Файлы `.doc` сначала конвертируются в `.docx` через LibreOffice headless отдельным подготовительным этапом.
- JSON генерируется только из подготовленных `.docx`, а не напрямую из `.doc`.
- `data/pdf-processing-map.json` — legacy-файл для старого PDF-парсера; новый Word pipeline его не использует.
- Reviewed JSON в `data/parsed/{year}/` — источник правды для приложения.
- `DOCUMENTS_GUIDE.md` — источник правил по типам документов, трем датам, заголовку, телу и футеру.
- Postgres — runtime index, который всегда можно пересобрать из JSON.
- MVP режется до минимально работающего публичного каталога.
- Все необязательные фичи, которые тормозят релиз, переносятся после деплоя.
- Каждый этап закрывается отдельным коммитом.
- После завершения этапа отмечаем чекбоксы в этом файле.

## MVP Scope

Первый публичный релиз должен делать только это:

- показать общий список материалов;
- сгруппировать материалы по году и месяцу публикации на сайте;
- сортировать новые материалы выше старых;
- дать фильтр только по году публикации на сайте;
- открыть страницу материала для чтения;
- для составных Word-брошюр показать на карточке выпуска найденные внутренние диктовки/лекции: автор + название, насколько это надежно извлекается;
- генерировать индивидуальные TXT/DOCX/EPUB скачивания отдельной deploy-time командой;
- использовать Postgres как runtime index, пересобираемый из reviewed JSON;
- иметь нормальный дизайн, достаточный для публичного запуска;
- задеплоиться в production.

Не входит в MVP:

- фильтр по автору;
- страницы авторов;
- фильтр по историческому году создания;
- фильтр по типу документа;
- полноценное разбиение составной брошюры на отдельные публичные страницы;
- общий архив всех документов;
- bulk ZIP downloads всех документов;
- RAG, чат, embeddings, Qdrant;
- админка или модераторская панель.

## Текущее Решение — Word-First Источник

Статус: подтверждено. Старый PDF-first путь считается историческим и не является направлением ближайшей реализации.

Стартовый набор для реализации и проверки:

```text
data/source-data/pearls-word/2022/1-й квартал/Брошюры
data/source-data/pearls-word/2022/2-й квартал/БРОШЮРЫ
data/source-data/pearls-word/2022/3-й квартал/БРОШЮРЫ
data/source-data/pearls-word/2022/4-й квартал/Брошюры
```

После проверки 2022 года тот же обход должен работать для всех годов и кварталов, где есть папки `Брошюры` или `БРОШЮРЫ`, например:

```text
data/source-data/pearls-word/2026/1-й квартал/Брошюры
data/source-data/pearls-word/2023/3-й квартал/Брошюры
```

Цель ближайшего этапа: получить воспроизводимый pipeline `raw Word -> prepared DOCX -> reviewed JSON -> Postgres`.

- [x] Перенести архив PDF в `data/source-data/pearls-pdf/`
- [ ] Добавить обход всех папок `data/source-data/pearls-word/<год>/<квартал>/Брошюры` и `data/source-data/pearls-word/<год>/<квартал>/БРОШЮРЫ`
- [ ] Игнорировать `Оригиналы`, английские исходники и временные файлы `~$...`
- [ ] Добавить подготовительный CLI для конвертации `.doc` в `.docx` через LibreOffice
- [ ] Складывать подготовленные `.docx` в `data/word-docx/{year}/{quarter}/Брошюры/`
- [ ] Сохранять исходную структуру по годам и кварталам в `data/word-docx/`
- [ ] Не перезаписывать существующий `.docx`, если исходный файл не изменился
- [ ] Добавить Word-парсер поверх подготовленных `.docx` через `mammoth`
- [ ] Сохранить модель: одна брошюра = одна жемчужина = один JSON
- [ ] Сохранять внутренние материалы брошюры в `documents[]`
- [ ] Записать в JSON путь к исходному Word-файлу и подготовленному DOCX
- [ ] Убрать PDF-специфику из новой генерации JSON: колонки, layout, `pdf-processing-map`
- [ ] Сгенерировать JSON для 1-го квартала 2022 и вручную проверить качество
- [ ] После проверки сгенерировать JSON для всех кварталов 2022
- [ ] После проверки 2022 года прогнать все найденные годы/кварталы
- [ ] Обновить seed и каталог под новое поле источника

План коммитов:

1. `Зафиксировать переход на Word-брошюры`
2. `Перенести архив PDF в data`
3. `Добавить подготовку DOCX`
4. `Добавить парсинг подготовленных DOCX`
5. `Сгенерировать данные за 2022 год`
6. `Прогнать Word-архив по всем годам`

## Sprint 1 — Нормализация JSON

Статус: закрыто. Парсер обновлен, `data/parsed/` перегенерирован, reviewed JSON отслеживается git, структура всех JSON проверена.

Цель: сделать JSON самодостаточным источником данных для DB seed, каталога, sitemap и будущих этапов.

Дополнение: нормализация должна учитывать документную модель из `DOCUMENTS_GUIDE.md`. В JSON нужно явно разделить редакционную дату сайта, историческую дату создания и дату публикации в `Жемчужинах Мудрости`.

- [x] Проверить текущие изменения парсера дат
- [x] Довести `PearlDocument` до целевой формы
- [x] Добавить в JSON поля `slug`, `year`, `month`, `day`, `publishedAt`, `sortDate`
- [x] Добавить в legacy JSON поля `speaker`, `sourcePdf`, `jsonPath`, `parsedAt`
- [x] Изменить output parser: `data/parsed/{year}/{originalName}.json`
- [x] Перегенерировать `data/parsed/` автоматически, без ручной правки каждого файла
- [x] Локально проверить 10 случайных JSON
- [x] Обновить `PearlDocument` под `documentType`, `author`, `sitePublication`, `creation`, `pearlPublication`
- [x] Разделить текст на `parts.header`, `parts.body`, `parts.footer`
- [x] Сохранять футер после черты отдельно и использовать его для уточнения автора, типа и даты создания
- [x] Научить парсер считать `Открывающий призыв`, `Призыв`, `Молитва` и похожие строки началом тела, а не продолжением заголовка
- [x] Извлекать строку `Том ... № ...` в `pearlPublication`, если она присутствует
- [x] Закоммитить reviewed JSON

План коммитов:

1. `Нормализовать метаданные документов`
2. `Обновить распарсенные данные`

## Sprint 2 — MVP Catalog Core

Цель: получить надежный общий каталог для первого релиза. Главная страница, sitemap и lookup материалов читают каталог из Postgres, а не сканируют JSON на runtime.

- [x] Добавить локальный Postgres setup
- [x] Установить Prisma
- [x] Создать `Lecture` model под актуальную JSON-модель
- [x] Написать seed из `data/parsed/`
- [x] Переключить главную страницу на DB catalog с сортировкой по `sitePublication.sortDate`
- [x] Переключить sitemap на DB
- [x] Оставить полный текст, `parts` и `paragraphs` в JSON
- [x] Добавить фильтры по автору, году публикации сайта, году создания и типу документа
- [ ] Для MVP скрыть все публичные фильтры кроме года публикации сайта

### Sprint 2.1 — Надежная Дата Сайта И Фильтр По Году

Цель: перед первым публичным релизом оставить в интерфейсе только надежную навигацию по дате публикации на сайте.

Причина: Word-брошюры дают более надежную структуру публикации, но месяц и квартал всё равно нужно фиксировать воспроизводимо: из папки квартала, имени брошюры и первых строк документа.

- [ ] Считать год публикации сайта из папки `data/source-data/pearls-word/{year}/` / `data/parsed/{year}/`
- [ ] Считать квартал из папки `1-й квартал`, `2-й квартал`, `3-й квартал`, `4-й квартал`
- [ ] Считать месяц публикации сайта из имени брошюры (`январь`, `февраль`, `март` и т.д.)
- [ ] Использовать первые строки документа как дополнительный источник, если имя файла неоднозначное
- [ ] Проверить `sitePublication` на всех 12 брошюрах 2022 года
- [ ] Временно убрать из UI фильтры по автору, году создания и типу документа
- [ ] Оставить публичный фильтр только по году публикации на сайте
- [ ] Оставить группировку списка по году и месяцу публикации на сайте
- [ ] Пересидить Postgres из исправленных JSON

План коммитов:

1. `Уточнить дату публикации сайта`
2. `Оставить фильтр по году сайта`

### Sprint 2.2 — Составные Брошюры В MVP

Цель: не дробить составную Word-брошюру на отдельные страницы, но показать в списке, какие внутренние материалы в ней есть.

Минимальная модель для MVP:

- одна Word-брошюра остается одной карточкой и одной страницей чтения;
- внутри JSON появляется или вычисляется список внутренних материалов для отображения;
- для каждого внутреннего материала пытаемся показать `author`, `title`, `rawHeader`;
- если автор или название не уверены, показываем безопасную исходную строку, а не выдуманное значение;
- эти данные используются только для отображения в карточке/странице, не для фильтров.

Маркерный подход:

- искать длинный разделитель футера (`_____`, `-----` и похожие);
- после футера ожидать новый блок коротких строк-заголовков;
- искать строки вида `Диктовка ...`, `Лекция ...`, `Проповедь ...`;
- строку `Том ... № ...` использовать как слабый дополнительный сигнал, но не как обязательный критерий;
- не подключать модель на этом этапе.

- [ ] Найти реальные составные брошюры в 2022 году и выбрать 5-10 контрольных примеров
- [ ] Добавить минимальное извлечение внутренних заголовков/авторов для карточки выпуска
- [ ] Обновить типы JSON/DB или runtime DTO под список `containedDocuments`
- [ ] Вывести внутренние заголовки на карточке каталога
- [ ] Вывести внутренние заголовки на странице материала
- [ ] Проверить все брошюры 2022 года после первого Word-парсинга
- [ ] Пересидить Postgres из исправленных JSON

План коммитов:

1. `Найти заголовки внутри составных выпусков`
2. `Показать состав выпусков в каталоге`

## Sprint 3 — MVP Design

Цель: довести публичный вид до нормального минимального сайта.

- [ ] Упростить карточку материала под MVP: дата сайта, заголовок выпуска, внутренние заголовки
- [ ] Сделать годовой фильтр и активный фильтр понятными
- [ ] Сгруппировать список по годам и месяцам
- [ ] Улучшить страницу чтения: ширина текста, отступы, типографика
- [ ] Проверить мобильный вид
- [ ] Проверить пустые/неуверенные метаданные, чтобы UI не выглядел сломанным

План коммитов:

1. `Упростить карточки каталога`
2. `Обновить дизайн чтения`

## Sprint 4 — Production Deploy

Цель: production flow через PM2 deploy и Postgres.

- [ ] Настроить production env
- [ ] Настроить PM2 deploy
- [ ] Прогонять Prisma migrations на production Postgres
- [ ] Сидить Postgres из committed JSON
- [ ] Запускать `npm run generate:downloads` после seed
- [ ] Добавить `/health`
- [ ] Проверить production список по годам
- [ ] Проверить открытие нескольких старых и новых материалов

План коммитов:

1. `Добавить production env пример`
2. `Настроить PM2 deploy`
3. `Добавить healthcheck`

## Post-MVP Backlog

Все пункты ниже не блокируют первый публичный релиз.

### Legacy Cleanup

Файлы и модули, которые нужно удалить после завершения Word pipeline и окончательного отказа от PDF-парсинга:

- [ ] `data/pdf-processing-map.json` — карта ручных override для старого PDF-парсера; новый Word pipeline ее не использует.

### Bulk Archives

Цель: добавить общие ZIP-архивы всех документов по форматам.

- [ ] Генерировать `var/downloads/bundles/all-txt.zip`
- [ ] Генерировать `var/downloads/bundles/all-docx.zip`
- [ ] Генерировать `var/downloads/bundles/all-epub.zip`
- [ ] Добавить route для bulk ZIP downloads
- [ ] Добавить 3 кнопки bulk downloads в конце каталога
- [ ] Показывать размер каждого ZIP, если файл уже сгенерирован

### Search & RAG

Цель: подготовить поиск и будущий RAG поверх reviewed JSON и Postgres catalog.

- [ ] Добавить Postgres FTS
- [ ] Добавить chunker
- [ ] Добавить Qdrant setup
- [ ] Добавить embedding scripts
- [ ] Добавить `POST /api/ask`
- [ ] Добавить `/chat`

План коммитов:

1. `Добавить полнотекстовый поиск`
2. `Добавить разбиение лекций на чанки`
3. `Добавить индексацию в Qdrant`
4. `Добавить RAG endpoint`
5. `Добавить страницу чата`

### Authors Reference

Цель: добавить справочник авторов после первого деплоя, правки дизайна и стабилизации каталога.

Справочник авторов должен стать отдельным источником нормализации имен и страниц авторов, а не эвристикой поверх текущих смешанных метаданных.

- [ ] Получить и сохранить исходную книгу/справочник авторов
- [ ] Определить формат `authors` dataset: canonical name, aliases, slug, biography, source refs
- [ ] Распарсить или вручную подготовить первичный список авторов
- [ ] Добавить страницы `/authors/{slug}`
- [ ] Добавить ссылки на автора из карточки материала и страницы материала
- [ ] Использовать aliases из справочника для нормализации автора при парсинге документов
- [ ] Вернуть публичный фильтр по автору после проверки качества сопоставления

План коммитов:

1. `Добавить справочник авторов`
2. `Добавить страницы авторов`
3. `Связать материалы с авторами`

## Quarterly Content Release Flow

Повторяемый флоу для новых квартальных пакетов Word-брошюр:

1. Получить новые Word-брошюры от редакторов.
2. Положить файлы в `data/source-data/pearls-word/{year}/{quarter}/Брошюры`.
3. Запустить подготовку DOCX: `.doc` конвертируется в `.docx`, готовые `.docx` копируются как есть.
4. Проверить подготовленные файлы в `data/word-docx/{year}/`.
5. Запустить генерацию JSON из `data/word-docx/{year}/`.
6. Проверить JSON в `data/parsed/{year}/`.
7. Запустить seed в локальный Postgres.
8. Если JSON и каталог корректны, сделать коммит.
9. Запушить в `main`.
10. Задеплоить через PM2.
11. Запустить seed на production Postgres.
12. Проверить каталог по годам и чтение материалов.

План коммита для контентного релиза:

`Добавить лекции за YYYY QN`
